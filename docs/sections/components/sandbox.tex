% TikZ -> "ist kein Zeichenprogramm", or
% TikZ is not a drawing program
% pgf -> portable graphic format pgf

% \posnexLineOne
% \posnexOrangeCircleOne

% \posnexTriangleOne
% \posnexLineAnglesTwo
% \posnexLinePlusPlusThree
%\posnexTriangleThree\posnexTriangleTwo
%\posnexLineThree


%[edge from parent fork down, sibling distance=15mm, level distance=15mm,
%every node/.style={fill=red!30,rounded corners},edge from parent/.style={red,-{Circle[open]},thick,draw}]

% Horizontally Centered: Uses the -| operator to horizontally center the content.
% Vertically Centered: Uses the |- operator to vertically center the content.
\begin{tikzpicture}[overlay, remember picture]
    \tikzset{
        directory/.style={
            draw=gray, % Border color
            fill=blue, % Fill color
            rounded corners, % Rounded edges
            text centered,
            font=\sffamily\bfseries
        },
        file/.style={
            draw=gray, % Border color
            fill=olive, % Fill color
            rounded corners, % Rounded edges
            text centered,
            font=\sffamily\bfseries
        },
        symlink/.style={
            fill=teal, % Fill color
            rounded corners, % Rounded edges
            text centered,
            font=\sffamily\bfseries
        }
    }

    \node at (current page.center) {
        \begin{tikzpicture}[
            sibling distance=2cm,
            level distance=1cm,
            anchor=west,
            grow=east]
            \node[directory] {/} child {
                child {node[directory]{var}
                    child {
                        child {node[directory]{log}
                            child {
                                child {node[directory]{posix-nexus}}
                            }
                        }
                        child {node[directory]{run}
                            child {
                                child {node[directory]{posix-nexus}
                                    child {
                                        child {node[directory]{posix-nexus.pid}}
                                    }
                                    child[draw,dashed,thick] {
                                        child {node[symlink]{posix-nexus-directory}}
                                    }
                                }
                            }
                        }
                        child {node[directory] at (0mm,5mm){tmp}
                            child {
                                child {node[directory]{posix-nexus}
                                    child {
                                        child {node[directory]{\$!}}
                                    }
                                }
                            }
                        }
                    }
                }
                child {node[directory] at (0mm,25mm){usr}
                    child {
                        child {node[directory]{local}
                            child {
                                child {node[directory]{posix-nexus}
                                    child {
                                        child {node[directory] at (10mm,10mm){main}}
                                        child {node[directory] at (10mm,1mm){docs}}
                                        child {node[file] at (10mm,-8mm){run.sh}}
                                    }
                                }
                            }
                        }
                    }
                }
            };       
        \end{tikzpicture}
    };
\end{tikzpicture}


% \begin{tikzpicture}
%     \node (start) [draw,shape=rectangle] {/};
%     \foreach \angle in {-90, -80, ..., 90}
%     \draw (node cs:name=start,angle=\angle)
%     .. controls +(\angle:1cm) and +(-1,0) .. (2.5,0);
% \end{tikzpicture}

