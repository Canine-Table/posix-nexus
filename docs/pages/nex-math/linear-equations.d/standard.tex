\nxSections{Standard Form}{2}

\begin{empheq}[box=\nxSuccessMathBox]{align}
    Ax + By = C
\end{empheq}
\bigskip

\begin{NxLightListBox}[title={Standard Form}]
    \nxEachLabel{ArrowDark}{Secondary}{{4}{5}}{%
        {Form}/{\( Ax + By = C \)},%
        {Coefficients}/{\(A, B, C\) are integers with \(A \geq 0\)},%
        {Slope}/{Slope is \( -\frac{A}{B} \)},%
        {Intercepts}/{Find intercepts by setting \(x=0\) or \(y=0\)},%
        {Outcome}/{Useful for quickly finding intercepts and handling integer coefficients}%
    }
\end{NxLightListBox}

