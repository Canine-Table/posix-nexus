\newpage
\section{Animation}
\label{Animation}
\begin{NexMainBox}[light, hdrA, sdwA, crnA, grwB, secA]
	\begin{NexMainBox}[dark, crnA]]
		The following functions enhance user interface customization by dynamically managing themes and integrating responsive design preferences.
	\end{NexMainBox}
	\begin{NexMainBox}[dark, crnA]
		\begin{NexListDark}
			\NexItemDark{\NexFunction{nexAnime.setTheme(val)}: Adjusts the current theme based on the specified value \NexFunction{val}. If no value is provided, determines the theme by checking session storage, user preference, or system settings. Ensures the theme is stored and updated by applying the relevant attribute to the root element using \NexFunction{nexNode.SetAttr}. Returns the selected theme.}
		\end{NexListDark}
	\end{NexMainBox}
\end{NexMainBox}

