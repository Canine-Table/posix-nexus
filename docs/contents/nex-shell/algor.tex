\newpage
\section{Algorithms}
\label{Algorithms}
\begin{NexMainBox}[light, hdrA, sdwA, crnA, grwB, secA]
	\begin{NexMainBox}[dark, crnA]]
		The following functions utilize efficient algorithms for sorting and processing data structures, with a focus on modularity and adaptability.
	\end{NexMainBox}
	\begin{NexMainBox}[dark, crnA]
		\begin{NexListDark}
			\NexItemDark{\NexFunction{set\_algor\_qsort()}: Implements a QuickSort algorithm to sort a list (\NexFunction{lst}) of elements, with options to reverse the sort order (\NexFunction{rvs}), apply a custom sorting mechanism (\NexFunction{meh}), and use specified delimiters (\NexFunction{sep} and \NexFunction{osep}).}
		\end{NexListDark}
	\end{NexMainBox}
\end{NexMainBox}

