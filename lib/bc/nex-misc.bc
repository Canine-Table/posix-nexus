define nx_else(x, y) {
	if (x == 0 || -x == 0)
		return y
	return x
}

define nx_if(x, y, z) {
	if (x == 0 || -x = 0)
		return z
	return y
}

define nx_xy_breach(x, y) {
	if (y < 0 || x < 0) {
		print "<nx:impurity/>"
		return -1
	}
}
