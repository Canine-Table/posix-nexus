BEGIN {
    while (1) {
        # while ((getline line) > 0) {
            print $0;

#            fflush(ENVIRON["POSIX_NEXUS_STDOUT"])
        # }

    } 

#    close(ENVIRON["POSIX_NEXUS_STDIN"])
}
